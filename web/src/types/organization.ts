/** A organization: the top of the org hierarchy (organization → project → environment). */
export interface Organization {
  id: string
  slug: string
  name: string
  created_at: string
  updated_at: string
}
